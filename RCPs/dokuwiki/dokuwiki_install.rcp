#
# Sample Recipe script
#

prepare(){ # Run on local
:
  # : ${__MATERIALS:="${HOME}/Materials"}           # material dir
  # echo "${__TGT}"
}

main(){ # Run on remote
ls -l /
}

afterwords(){ # Run on local
:
}
